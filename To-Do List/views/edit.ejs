<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Task</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <style>
        /* General Reset */
body {
    font-family: 'Poppins', sans-serif;
    background-color: #f8f9fa;
    color: #333;
    margin: 0;
    padding: 0;
}

/* Centered Container */
.container {
    max-width: 450px;
    background: #fff;
    padding: 30px;
    border-radius: 10px;
    box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
    margin-top: 60px;
}

/* Heading Style */
h2 {
    font-size: 24px;
    font-weight: 600;
    color: #343a40;
    text-align: center;
    margin-bottom: 20px;
}

/* Form Styling */
form input {
    height: auto;
    border-radius: 8px;
    border: 1px solid #ced4da;
    padding: 10px;
    font-size: 16px;
    transition: 0.3s;
}

form input:focus {
    border-color: #28a745;
    box-shadow: 0 0 5px rgba(40, 167, 69, 0.3);
}

/* Buttons Styling */
.btn {
    font-size: 16px;
    font-weight: 500;
    border-radius: 8px;
    transition: 0.3s;
}

.btn-success {
    background-color: #28a745;
    border-color: #28a745;
}

.btn-success:hover {
    background-color: #218838;
    border-color: #1e7e34;
}

.btn-secondary {
    background-color: #6c757d;
    border-color: #6c757d;
}

.btn-secondary:hover {
    background-color: #5a6268;
    border-color: #545b62;
}

/* Responsive Design */
@media (max-width: 576px) {
    .container {
        max-width: 90%;
        padding: 20px;
    }

    .btn {
        width: 100%;
        margin-top: 10px;
    }
}

    </style>
</head>
<body>
    <div class="container mt-5">
        <h2 class="text-center">Edit Task</h2>
        
        <form action="/updateTask/<%= task.id %>" method="POST" class="mb-4">
            <input type="text" name="task" class="form-control mb-2" value="<%= task.task %>" required>
            <button type="submit" class="btn btn-success">Update Task</button>
            <a href="/" class="btn btn-secondary ms-2">Cancel</a>
        </form>
    </div>
</body>
</html>