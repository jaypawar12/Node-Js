<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= movie.movieName %> - Details
    </title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
    <style>
        body {
            background: linear-gradient(to bottom, #0f2027, #203a43, #2c5364);
            color: #ffffff;
            font-family: 'Poppins', sans-serif;
            margin: 0;
            padding: 0;
        }

        .movie-details-container {
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 40px;
        }

        .movie-card {
            display: flex;
            flex-wrap: wrap;
            background: rgba(0, 0, 0, 0.8);
            border-radius: 20px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.5);
            overflow: hidden;
            width: 100%;
            max-width: 1200px;
        }

        .movie-image {
            flex: 1;
            max-width: 500px;
            overflow: hidden;
            border-radius: 20px 0 0 20px;
        }

        .movie-image img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            transition: transform 0.5s ease;
        }

        .movie-image img:hover {
            transform: scale(1.1);
        }

        .movie-content {
            flex: 2;
            padding: 25px;
            color: #e0e0e0;
            display: flex;
            flex-direction: column;
            justify-content: center;
        }

        .movie-title {
            text-transform: capitalize;
            font-size: 3rem;
            font-weight: bold;
            margin-bottom: 20px;
            color: #f92525;
            text-shadow: 0 2px 5px rgba(0, 0, 0, 0.5);
        }

        .movie-info {
            font-size: 1.2rem;
            margin-bottom: 15px;
            display: flex;
            align-items: center;
            text-transform: capitalize;
        }

        .movie-info i {
            margin-right: 10px;
        }

        .movie-description {
            text-transform: capitalize;
            font-size: 1.1rem;
            line-height: 1.8;
            margin-bottom: 20px;
            color: #d1d1d1;
        }

        .rating {
            font-size: 1.4rem;
            color: #ffca28;
        }

        .rating i {
            color: #ffca28;
        }

        .action-buttons {
            margin-top: 20px;
        }

        .btn-edit, .btn-delete {
            color: white;
            font-weight: 600;
            font-size: 1rem;
            padding: 7px 20px;
            border: none;
            text-transform: capitalize;
            transition: all 0.3s ease;
            margin-right: 10px;
        }

        .btn-edit{
            border: 2px solid #43a047;
        }

        .btn-edit:hover {
            color: black;
            background-color: #388e3c;
            transform: translateY(-3px);
        }

        .btn-delete {
            border: 2px solid #d32f2f;
        }

        .btn-delete:hover {
            color: black;
            background-color: #b71c1c;
            transform: translateY(-3px);
        }

        .btn-back {
            width: 25%;
            color: white;
            font-size: 1rem;
            font-weight: 600;
            padding: 7px 5px;
            border: 2px solid #0ae4f4;
            text-transform: capitalize;
            transition: all 0.3s ease;
            margin-top: 20px;
        }

        .btn-back:hover {
            color: black;
            transform: translateY(-3px);
            background-color: #0ae4f4;
        }
    </style>
</head>

<body>
    <div class="container movie-details-container">
        <div class="movie-card">
            <!-- Left Side: Movie Image -->
            <div class="movie-image">
                <img src="../<%= movie.movieImage %>" alt="<%= movie.movieName %>">
            </div>

            <!-- Right Side: Movie Details -->
            <div class="movie-content">
                <h2 class="movie-title">
                    <%= movie.movieName %>
                </h2>
                <div class="movie-info">
                    <i class="bi bi-cash-coin"></i> ₹<%= movie.ticketPrice %>
                </div>
                <div class="movie-info">
                    <i class="bi bi-film"></i>
                    <%= movie.genre %>
                </div>
                <p class="movie-description">
                    Description : <%= movie.description %>
                </p>
                <div class="rating">
                    Rating : <%= movie.rating %> <i class="bi bi-star-fill"></i>
                </div>
                <div class="action-buttons">
                    <a href="/updateMovie/<%= movie.id %>" class="btn btn-edit">Edit</a>
                    <a href="/delete/delMovie?id=<%= movie.id %>" class="btn btn-delete">Delete</a>
                </div>
                <a href="/" class="btn btn-back">Back To Home Page <i class="bi bi-arrow-right"></i></a>
            </div>
        </div>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>